version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npx prisma@6.14.0 generate
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
